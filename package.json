{
  "name": "board",
  "version": "1.0.0",
  "description": "Elm Back-end framework",
  "main": "app.elm",
  "scripts": {
    "start": "nodemon --exec \"elm make src/port.elm --output dist/main.js && node local.js\"",
    "build": "elm make src/app.elm --output=dist/main.js",
    "install": "elm-package install elm-community/dict-extra",
    "elm": "elm",
    "test": "elm-test --watch"
  },
  "author": "AIRTucha",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.18.0",
    "object-hash": "^1.3.0"
  },
  "devDependencies": {
    "elm-test": "^0.18.12",
    "nodemon": "^1.12.1"
  }
}
